apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgresql.metadata.name }}-secret
  namespace:  {{ .Values.postgresql.namespace }}
  annotations:
   "helm.sh/hook": pre-install
data:
 {{ .Values.postgresql.secret.key }} : {{ .Values.postgresql.secret.value }}